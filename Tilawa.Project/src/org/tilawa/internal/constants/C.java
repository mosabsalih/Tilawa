package org.tilawa.internal.constants;

public interface C {

	public enum UserType {
		STUDENT, TEACHER	
	}
	
	public enum TimeUnit {
		HOUR, DAY, MONTH, YEAR
	}
	
	public enum SystemStatus {
		CREATED, ACTIVE, COMPLETED, INACTIVE, DELETED
	}
		
}
